import { configureStore } from "@reduxjs/toolkit";

export const store = configureStore({
    reducer: {
        // auth, book, borrow, and user slices get added here in the coming sections
    },
}); 